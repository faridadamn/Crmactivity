CRM Activity

Single-file HTML CRM app for sales activity, prospects, pipeline, and follow-up.

The file crm-activity.html is the baseline for subsequent iterations.

Open it in a browser. Data is saved in localStorage.
